import { InvestigationFormProps } from "@/types/commonTypes";

export default function CRPForm({ investigationRegisterId, investigationId }: InvestigationFormProps) {
    return (
        <div>CRP Form</div>
    );
}