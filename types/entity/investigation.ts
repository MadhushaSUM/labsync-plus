export interface InvestigationType {
    id: number;
    name: string,
    code: string,
    specimen: string
}

export interface Test {
    id: number;
    name: string;
    code: string;
    price: number;
    version: number;
}